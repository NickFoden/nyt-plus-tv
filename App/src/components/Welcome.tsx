import React from 'react';
import {
  // Image,
  Text,
  View,
} from 'react-native';
// import Logo from "../static/svg/logo-icon.svg"

const Welcome = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems:'center'}}>
      <Text style={{fontSize: 100}}>Welcome</Text>
    </View>
  );
};

export default Welcome;
